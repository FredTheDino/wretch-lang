mod Example

typ B = Int

dat Maybe a = Just a | Nothing

def : B, Int -> Int
def add = +

def : Int -> Int
def fac = 
  . if 1 <
    then ^ 1
    else . 1 - fac *

def : List Int -> Maybe Int
def head = ...
